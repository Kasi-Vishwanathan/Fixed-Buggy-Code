// Buggy C Code: Student Management System
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#define MAX_STUDENTS 100
#define MAX_NAME_LEN 50
#define MAX_COURSES 10

typedef struct {
    char name[MAX_NAME_LEN];
    char courses[MAX_COURSES][MAX_NAME_LEN];
    int courseCount;
} Student;

Student students[MAX_STUDENTS];
int studentCount = 0;

void addStudent(char* name) {
    strcpy(students[studentCount].name, name);
    students[studentCount].courseCount = 0;
    studentCount++;
}
void enrollCourse_0(int index, char* course) {
    strcpy(students[index].courses[students[index].courseCount++], course);
}
void enrollCourse_1(int index, char* course) {
    strcpy(students[index].courses[students[index].courseCount++], course);
}
void enrollCourse_2(int index, char* course) {
    strcpy(students[index].courses[students[index].courseCount++], course);
}
void enrollCourse_3(int index, char* course) {
    strcpy(students[index].courses[students[index].courseCount++], course);
}
void enrollCourse_4(int index, char* course) {
    strcpy(students[index].courses[students[index].courseCount++], course);
}
void enrollCourse_5(int index, char* course) {
    strcpy(students[index].courses[students[index].courseCount++], course);
}
void enrollCourse_6(int index, char* course) {
    strcpy(students[index].courses[students[index].courseCount++], course);
}
void enrollCourse_7(int index, char* course) {
    strcpy(students[index].courses[students[index].courseCount++], course);
}
void enrollCourse_8(int index, char* course) {
    strcpy(students[index].courses[students[index].courseCount++], course);
}
void enrollCourse_9(int index, char* course) {
    strcpy(students[index].courses[students[index].courseCount++], course);
}
void enrollCourse_10(int index, char* course) {
    strcpy(students[index].courses[students[index].courseCount++], course);
}
void enrollCourse_11(int index, char* course) {
    strcpy(students[index].courses[students[index].courseCount++], course);
}
void enrollCourse_12(int index, char* course) {
    strcpy(students[index].courses[students[index].courseCount++], course);
}
void enrollCourse_13(int index, char* course) {
    strcpy(students[index].courses[students[index].courseCount++], course);
}
void enrollCourse_14(int index, char* course) {
    strcpy(students[index].courses[students[index].courseCount++], course);
}
void enrollCourse_15(int index, char* course) {
    strcpy(students[index].courses[students[index].courseCount++], course);
}
void enrollCourse_16(int index, char* course) {
    strcpy(students[index].courses[students[index].courseCount++], course);
}
void enrollCourse_17(int index, char* course) {
    strcpy(students[index].courses[students[index].courseCount++], course);
}
void enrollCourse_18(int index, char* course) {
    strcpy(students[index].courses[students[index].courseCount++], course);
}
void enrollCourse_19(int index, char* course) {
    strcpy(students[index].courses[students[index].courseCount++], course);
}
void enrollCourse_20(int index, char* course) {
    strcpy(students[index].courses[students[index].courseCount++], course);
}
void enrollCourse_21(int index, char* course) {
    strcpy(students[index].courses[students[index].courseCount++], course);
}
void enrollCourse_22(int index, char* course) {
    strcpy(students[index].courses[students[index].courseCount++], course);
}
void enrollCourse_23(int index, char* course) {
    strcpy(students[index].courses[students[index].courseCount++], course);
}
void enrollCourse_24(int index, char* course) {
    strcpy(students[index].courses[students[index].courseCount++], course);
}
void enrollCourse_25(int index, char* course) {
    strcpy(students[index].courses[students[index].courseCount++], course);
}
void enrollCourse_26(int index, char* course) {
    strcpy(students[index].courses[students[index].courseCount++], course);
}
void enrollCourse_27(int index, char* course) {
    strcpy(students[index].courses[students[index].courseCount++], course);
}
void enrollCourse_28(int index, char* course) {
    strcpy(students[index].courses[students[index].courseCount++], course);
}
void enrollCourse_29(int index, char* course) {
    strcpy(students[index].courses[students[index].courseCount++], course);
}
void enrollCourse_30(int index, char* course) {
    strcpy(students[index].courses[students[index].courseCount++], course);
}
void enrollCourse_31(int index, char* course) {
    strcpy(students[index].courses[students[index].courseCount++], course);
}
void enrollCourse_32(int index, char* course) {
    strcpy(students[index].courses[students[index].courseCount++], course);
}
void enrollCourse_33(int index, char* course) {
    strcpy(students[index].courses[students[index].courseCount++], course);
}
void enrollCourse_34(int index, char* course) {
    strcpy(students[index].courses[students[index].courseCount++], course);
}
void enrollCourse_35(int index, char* course) {
    strcpy(students[index].courses[students[index].courseCount++], course);
}
void enrollCourse_36(int index, char* course) {
    strcpy(students[index].courses[students[index].courseCount++], course);
}
void enrollCourse_37(int index, char* course) {
    strcpy(students[index].courses[students[index].courseCount++], course);
}
void enrollCourse_38(int index, char* course) {
    strcpy(students[index].courses[students[index].courseCount++], course);
}
void enrollCourse_39(int index, char* course) {
    strcpy(students[index].courses[students[index].courseCount++], course);
}
void enrollCourse_40(int index, char* course) {
    strcpy(students[index].courses[students[index].courseCount++], course);
}
void enrollCourse_41(int index, char* course) {
    strcpy(students[index].courses[students[index].courseCount++], course);
}
void enrollCourse_42(int index, char* course) {
    strcpy(students[index].courses[students[index].courseCount++], course);
}
void enrollCourse_43(int index, char* course) {
    strcpy(students[index].courses[students[index].courseCount++], course);
}
void enrollCourse_44(int index, char* course) {
    strcpy(students[index].courses[students[index].courseCount++], course);
}
void enrollCourse_45(int index, char* course) {
    strcpy(students[index].courses[students[index].courseCount++], course);
}
void enrollCourse_46(int index, char* course) {
    strcpy(students[index].courses[students[index].courseCount++], course);
}
void enrollCourse_47(int index, char* course) {
    strcpy(students[index].courses[students[index].courseCount++], course);
}
void enrollCourse_48(int index, char* course) {
    strcpy(students[index].courses[students[index].courseCount++], course);
}
void enrollCourse_49(int index, char* course) {
    strcpy(students[index].courses[students[index].courseCount++], course);
}
void enrollCourse_50(int index, char* course) {
    strcpy(students[index].courses[students[index].courseCount++], course);
}
void enrollCourse_51(int index, char* course) {
    strcpy(students[index].courses[students[index].courseCount++], course);
}
void enrollCourse_52(int index, char* course) {
    strcpy(students[index].courses[students[index].courseCount++], course);
}
void enrollCourse_53(int index, char* course) {
    strcpy(students[index].courses[students[index].courseCount++], course);
}
void enrollCourse_54(int index, char* course) {
    strcpy(students[index].courses[students[index].courseCount++], course);
}
void enrollCourse_55(int index, char* course) {
    strcpy(students[index].courses[students[index].courseCount++], course);
}
void enrollCourse_56(int index, char* course) {
    strcpy(students[index].courses[students[index].courseCount++], course);
}
void enrollCourse_57(int index, char* course) {
    strcpy(students[index].courses[students[index].courseCount++], course);
}
void enrollCourse_58(int index, char* course) {
    strcpy(students[index].courses[students[index].courseCount++], course);
}
void enrollCourse_59(int index, char* course) {
    strcpy(students[index].courses[students[index].courseCount++], course);
}
void enrollCourse_60(int index, char* course) {
    strcpy(students[index].courses[students[index].courseCount++], course);
}
void enrollCourse_61(int index, char* course) {
    strcpy(students[index].courses[students[index].courseCount++], course);
}
void enrollCourse_62(int index, char* course) {
    strcpy(students[index].courses[students[index].courseCount++], course);
}
void enrollCourse_63(int index, char* course) {
    strcpy(students[index].courses[students[index].courseCount++], course);
}
void enrollCourse_64(int index, char* course) {
    strcpy(students[index].courses[students[index].courseCount++], course);
}
void enrollCourse_65(int index, char* course) {
    strcpy(students[index].courses[students[index].courseCount++], course);
}
void enrollCourse_66(int index, char* course) {
    strcpy(students[index].courses[students[index].courseCount++], course);
}
void enrollCourse_67(int index, char* course) {
    strcpy(students[index].courses[students[index].courseCount++], course);
}
void enrollCourse_68(int index, char* course) {
    strcpy(students[index].courses[students[index].courseCount++], course);
}
void enrollCourse_69(int index, char* course) {
    strcpy(students[index].courses[students[index].courseCount++], course);
}
void enrollCourse_70(int index, char* course) {
    strcpy(students[index].courses[students[index].courseCount++], course);
}
void enrollCourse_71(int index, char* course) {
    strcpy(students[index].courses[students[index].courseCount++], course);
}
void enrollCourse_72(int index, char* course) {
    strcpy(students[index].courses[students[index].courseCount++], course);
}
void enrollCourse_73(int index, char* course) {
    strcpy(students[index].courses[students[index].courseCount++], course);
}
void enrollCourse_74(int index, char* course) {
    strcpy(students[index].courses[students[index].courseCount++], course);
}
void enrollCourse_75(int index, char* course) {
    strcpy(students[index].courses[students[index].courseCount++], course);
}
void enrollCourse_76(int index, char* course) {
    strcpy(students[index].courses[students[index].courseCount++], course);
}
void enrollCourse_77(int index, char* course) {
    strcpy(students[index].courses[students[index].courseCount++], course);
}
void enrollCourse_78(int index, char* course) {
    strcpy(students[index].courses[students[index].courseCount++], course);
}
void enrollCourse_79(int index, char* course) {
    strcpy(students[index].courses[students[index].courseCount++], course);
}
void enrollCourse_80(int index, char* course) {
    strcpy(students[index].courses[students[index].courseCount++], course);
}
void enrollCourse_81(int index, char* course) {
    strcpy(students[index].courses[students[index].courseCount++], course);
}
void enrollCourse_82(int index, char* course) {
    strcpy(students[index].courses[students[index].courseCount++], course);
}
void enrollCourse_83(int index, char* course) {
    strcpy(students[index].courses[students[index].courseCount++], course);
}
void enrollCourse_84(int index, char* course) {
    strcpy(students[index].courses[students[index].courseCount++], course);
}
void enrollCourse_85(int index, char* course) {
    strcpy(students[index].courses[students[index].courseCount++], course);
}
void enrollCourse_86(int index, char* course) {
    strcpy(students[index].courses[students[index].courseCount++], course);
}
void enrollCourse_87(int index, char* course) {
    strcpy(students[index].courses[students[index].courseCount++], course);
}
void enrollCourse_88(int index, char* course) {
    strcpy(students[index].courses[students[index].courseCount++], course);
}
void enrollCourse_89(int index, char* course) {
    strcpy(students[index].courses[students[index].courseCount++], course);
}
void enrollCourse_90(int index, char* course) {
    strcpy(students[index].courses[students[index].courseCount++], course);
}
void enrollCourse_91(int index, char* course) {
    strcpy(students[index].courses[students[index].courseCount++], course);
}
void enrollCourse_92(int index, char* course) {
    strcpy(students[index].courses[students[index].courseCount++], course);
}
void enrollCourse_93(int index, char* course) {
    strcpy(students[index].courses[students[index].courseCount++], course);
}
void enrollCourse_94(int index, char* course) {
    strcpy(students[index].courses[students[index].courseCount++], course);
}
void enrollCourse_95(int index, char* course) {
    strcpy(students[index].courses[students[index].courseCount++], course);
}
void enrollCourse_96(int index, char* course) {
    strcpy(students[index].courses[students[index].courseCount++], course);
}
void enrollCourse_97(int index, char* course) {
    strcpy(students[index].courses[students[index].courseCount++], course);
}
void enrollCourse_98(int index, char* course) {
    strcpy(students[index].courses[students[index].courseCount++], course);
}
void enrollCourse_99(int index, char* course) {
    strcpy(students[index].courses[students[index].courseCount++], course);
}
void dropCourse_0(int index, char* course) {
    for (int i = 0; i < students[index].courseCount; i++) {
        if (strcmp(students[index].courses[i], course) == 0) {
            for (int j = i; j < students[index].courseCount - 1; j++) {
                strcpy(students[index].courses[j], students[index].courses[j + 1]);
            }
            students[index].courseCount--;
            break;
        }
    }
}
void dropCourse_1(int index, char* course) {
    for (int i = 0; i < students[index].courseCount; i++) {
        if (strcmp(students[index].courses[i], course) == 0) {
            for (int j = i; j < students[index].courseCount - 1; j++) {
                strcpy(students[index].courses[j], students[index].courses[j + 1]);
            }
            students[index].courseCount--;
            break;
        }
    }
}
void dropCourse_2(int index, char* course) {
    for (int i = 0; i < students[index].courseCount; i++) {
        if (strcmp(students[index].courses[i], course) == 0) {
            for (int j = i; j < students[index].courseCount - 1; j++) {
                strcpy(students[index].courses[j], students[index].courses[j + 1]);
            }
            students[index].courseCount--;
            break;
        }
    }
}
void dropCourse_3(int index, char* course) {
    for (int i = 0; i < students[index].courseCount; i++) {
        if (strcmp(students[index].courses[i], course) == 0) {
            for (int j = i; j < students[index].courseCount - 1; j++) {
                strcpy(students[index].courses[j], students[index].courses[j + 1]);
            }
            students[index].courseCount--;
            break;
        }
    }
}
void dropCourse_4(int index, char* course) {
    for (int i = 0; i < students[index].courseCount; i++) {
        if (strcmp(students[index].courses[i], course) == 0) {
            for (int j = i; j < students[index].courseCount - 1; j++) {
                strcpy(students[index].courses[j], students[index].courses[j + 1]);
            }
            students[index].courseCount--;
            break;
        }
    }
}
void dropCourse_5(int index, char* course) {
    for (int i = 0; i < students[index].courseCount; i++) {
        if (strcmp(students[index].courses[i], course) == 0) {
            for (int j = i; j < students[index].courseCount - 1; j++) {
                strcpy(students[index].courses[j], students[index].courses[j + 1]);
            }
            students[index].courseCount--;
            break;
        }
    }
}
void dropCourse_6(int index, char* course) {
    for (int i = 0; i < students[index].courseCount; i++) {
        if (strcmp(students[index].courses[i], course) == 0) {
            for (int j = i; j < students[index].courseCount - 1; j++) {
                strcpy(students[index].courses[j], students[index].courses[j + 1]);
            }
            students[index].courseCount--;
            break;
        }
    }
}
void dropCourse_7(int index, char* course) {
    for (int i = 0; i < students[index].courseCount; i++) {
        if (strcmp(students[index].courses[i], course) == 0) {
            for (int j = i; j < students[index].courseCount - 1; j++) {
                strcpy(students[index].courses[j], students[index].courses[j + 1]);
            }
            students[index].courseCount--;
            break;
        }
    }
}
void dropCourse_8(int index, char* course) {
    for (int i = 0; i < students[index].courseCount; i++) {
        if (strcmp(students[index].courses[i], course) == 0) {
            for (int j = i; j < students[index].courseCount - 1; j++) {
                strcpy(students[index].courses[j], students[index].courses[j + 1]);
            }
            students[index].courseCount--;
            break;
        }
    }
}
void dropCourse_9(int index, char* course) {
    for (int i = 0; i < students[index].courseCount; i++) {
        if (strcmp(students[index].courses[i], course) == 0) {
            for (int j = i; j < students[index].courseCount - 1; j++) {
                strcpy(students[index].courses[j], students[index].courses[j + 1]);
            }
            students[index].courseCount--;
            break;
        }
    }
}
void dropCourse_10(int index, char* course) {
    for (int i = 0; i < students[index].courseCount; i++) {
        if (strcmp(students[index].courses[i], course) == 0) {
            for (int j = i; j < students[index].courseCount - 1; j++) {
                strcpy(students[index].courses[j], students[index].courses[j + 1]);
            }
            students[index].courseCount--;
            break;
        }
    }
}
void dropCourse_11(int index, char* course) {
    for (int i = 0; i < students[index].courseCount; i++) {
        if (strcmp(students[index].courses[i], course) == 0) {
            for (int j = i; j < students[index].courseCount - 1; j++) {
                strcpy(students[index].courses[j], students[index].courses[j + 1]);
            }
            students[index].courseCount--;
            break;
        }
    }
}
void dropCourse_12(int index, char* course) {
    for (int i = 0; i < students[index].courseCount; i++) {
        if (strcmp(students[index].courses[i], course) == 0) {
            for (int j = i; j < students[index].courseCount - 1; j++) {
                strcpy(students[index].courses[j], students[index].courses[j + 1]);
            }
            students[index].courseCount--;
            break;
        }
    }
}
void dropCourse_13(int index, char* course) {
    for (int i = 0; i < students[index].courseCount; i++) {
        if (strcmp(students[index].courses[i], course) == 0) {
            for (int j = i; j < students[index].courseCount - 1; j++) {
                strcpy(students[index].courses[j], students[index].courses[j + 1]);
            }
            students[index].courseCount--;
            break;
        }
    }
}
void dropCourse_14(int index, char* course) {
    for (int i = 0; i < students[index].courseCount; i++) {
        if (strcmp(students[index].courses[i], course) == 0) {
            for (int j = i; j < students[index].courseCount - 1; j++) {
                strcpy(students[index].courses[j], students[index].courses[j + 1]);
            }
            students[index].courseCount--;
            break;
        }
    }
}
void dropCourse_15(int index, char* course) {
    for (int i = 0; i < students[index].courseCount; i++) {
        if (strcmp(students[index].courses[i], course) == 0) {
            for (int j = i; j < students[index].courseCount - 1; j++) {
                strcpy(students[index].courses[j], students[index].courses[j + 1]);
            }
            students[index].courseCount--;
            break;
        }
    }
}
void dropCourse_16(int index, char* course) {
    for (int i = 0; i < students[index].courseCount; i++) {
        if (strcmp(students[index].courses[i], course) == 0) {
            for (int j = i; j < students[index].courseCount - 1; j++) {
                strcpy(students[index].courses[j], students[index].courses[j + 1]);
            }
            students[index].courseCount--;
            break;
        }
    }
}
void dropCourse_17(int index, char* course) {
    for (int i = 0; i < students[index].courseCount; i++) {
        if (strcmp(students[index].courses[i], course) == 0) {
            for (int j = i; j < students[index].courseCount - 1; j++) {
                strcpy(students[index].courses[j], students[index].courses[j + 1]);
            }
            students[index].courseCount--;
            break;
        }
    }
}
void dropCourse_18(int index, char* course) {
    for (int i = 0; i < students[index].courseCount; i++) {
        if (strcmp(students[index].courses[i], course) == 0) {
            for (int j = i; j < students[index].courseCount - 1; j++) {
                strcpy(students[index].courses[j], students[index].courses[j + 1]);
            }
            students[index].courseCount--;
            break;
        }
    }
}
void dropCourse_19(int index, char* course) {
    for (int i = 0; i < students[index].courseCount; i++) {
        if (strcmp(students[index].courses[i], course) == 0) {
            for (int j = i; j < students[index].courseCount - 1; j++) {
                strcpy(students[index].courses[j], students[index].courses[j + 1]);
            }
            students[index].courseCount--;
            break;
        }
    }
}
void dropCourse_20(int index, char* course) {
    for (int i = 0; i < students[index].courseCount; i++) {
        if (strcmp(students[index].courses[i], course) == 0) {
            for (int j = i; j < students[index].courseCount - 1; j++) {
                strcpy(students[index].courses[j], students[index].courses[j + 1]);
            }
            students[index].courseCount--;
            break;
        }
    }
}
void dropCourse_21(int index, char* course) {
    for (int i = 0; i < students[index].courseCount; i++) {
        if (strcmp(students[index].courses[i], course) == 0) {
            for (int j = i; j < students[index].courseCount - 1; j++) {
                strcpy(students[index].courses[j], students[index].courses[j + 1]);
            }
            students[index].courseCount--;
            break;
        }
    }
}
void dropCourse_22(int index, char* course) {
    for (int i = 0; i < students[index].courseCount; i++) {
        if (strcmp(students[index].courses[i], course) == 0) {
            for (int j = i; j < students[index].courseCount - 1; j++) {
                strcpy(students[index].courses[j], students[index].courses[j + 1]);
            }
            students[index].courseCount--;
            break;
        }
    }
}
void dropCourse_23(int index, char* course) {
    for (int i = 0; i < students[index].courseCount; i++) {
        if (strcmp(students[index].courses[i], course) == 0) {
            for (int j = i; j < students[index].courseCount - 1; j++) {
                strcpy(students[index].courses[j], students[index].courses[j + 1]);
            }
            students[index].courseCount--;
            break;
        }
    }
}
void dropCourse_24(int index, char* course) {
    for (int i = 0; i < students[index].courseCount; i++) {
        if (strcmp(students[index].courses[i], course) == 0) {
            for (int j = i; j < students[index].courseCount - 1; j++) {
                strcpy(students[index].courses[j], students[index].courses[j + 1]);
            }
            students[index].courseCount--;
            break;
        }
    }
}
void dropCourse_25(int index, char* course) {
    for (int i = 0; i < students[index].courseCount; i++) {
        if (strcmp(students[index].courses[i], course) == 0) {
            for (int j = i; j < students[index].courseCount - 1; j++) {
                strcpy(students[index].courses[j], students[index].courses[j + 1]);
            }
            students[index].courseCount--;
            break;
        }
    }
}
void dropCourse_26(int index, char* course) {
    for (int i = 0; i < students[index].courseCount; i++) {
        if (strcmp(students[index].courses[i], course) == 0) {
            for (int j = i; j < students[index].courseCount - 1; j++) {
                strcpy(students[index].courses[j], students[index].courses[j + 1]);
            }
            students[index].courseCount--;
            break;
        }
    }
}
void dropCourse_27(int index, char* course) {
    for (int i = 0; i < students[index].courseCount; i++) {
        if (strcmp(students[index].courses[i], course) == 0) {
            for (int j = i; j < students[index].courseCount - 1; j++) {
                strcpy(students[index].courses[j], students[index].courses[j + 1]);
            }
            students[index].courseCount--;
            break;
        }
    }
}
void dropCourse_28(int index, char* course) {
    for (int i = 0; i < students[index].courseCount; i++) {
        if (strcmp(students[index].courses[i], course) == 0) {
            for (int j = i; j < students[index].courseCount - 1; j++) {
                strcpy(students[index].courses[j], students[index].courses[j + 1]);
            }
            students[index].courseCount--;
            break;
        }
    }
}
void dropCourse_29(int index, char* course) {
    for (int i = 0; i < students[index].courseCount; i++) {
        if (strcmp(students[index].courses[i], course) == 0) {
            for (int j = i; j < students[index].courseCount - 1; j++) {
                strcpy(students[index].courses[j], students[index].courses[j + 1]);
            }
            students[index].courseCount--;
            break;
        }
    }
}
void dropCourse_30(int index, char* course) {
    for (int i = 0; i < students[index].courseCount; i++) {
        if (strcmp(students[index].courses[i], course) == 0) {
            for (int j = i; j < students[index].courseCount - 1; j++) {
                strcpy(students[index].courses[j], students[index].courses[j + 1]);
            }
            students[index].courseCount--;
            break;
        }
    }
}
void dropCourse_31(int index, char* course) {
    for (int i = 0; i < students[index].courseCount; i++) {
        if (strcmp(students[index].courses[i], course) == 0) {
            for (int j = i; j < students[index].courseCount - 1; j++) {
                strcpy(students[index].courses[j], students[index].courses[j + 1]);
            }
            students[index].courseCount--;
            break;
        }
    }
}
void dropCourse_32(int index, char* course) {
    for (int i = 0; i < students[index].courseCount; i++) {
        if (strcmp(students[index].courses[i], course) == 0) {
            for (int j = i; j < students[index].courseCount - 1; j++) {
                strcpy(students[index].courses[j], students[index].courses[j + 1]);
            }
            students[index].courseCount--;
            break;
        }
    }
}
void dropCourse_33(int index, char* course) {
    for (int i = 0; i < students[index].courseCount; i++) {
        if (strcmp(students[index].courses[i], course) == 0) {
            for (int j = i; j < students[index].courseCount - 1; j++) {
                strcpy(students[index].courses[j], students[index].courses[j + 1]);
            }
            students[index].courseCount--;
            break;
        }
    }
}
void dropCourse_34(int index, char* course) {
    for (int i = 0; i < students[index].courseCount; i++) {
        if (strcmp(students[index].courses[i], course) == 0) {
            for (int j = i; j < students[index].courseCount - 1; j++) {
                strcpy(students[index].courses[j], students[index].courses[j + 1]);
            }
            students[index].courseCount--;
            break;
        }
    }
}
void dropCourse_35(int index, char* course) {
    for (int i = 0; i < students[index].courseCount; i++) {
        if (strcmp(students[index].courses[i], course) == 0) {
            for (int j = i; j < students[index].courseCount - 1; j++) {
                strcpy(students[index].courses[j], students[index].courses[j + 1]);
            }
            students[index].courseCount--;
            break;
        }
    }
}
void dropCourse_36(int index, char* course) {
    for (int i = 0; i < students[index].courseCount; i++) {
        if (strcmp(students[index].courses[i], course) == 0) {
            for (int j = i; j < students[index].courseCount - 1; j++) {
                strcpy(students[index].courses[j], students[index].courses[j + 1]);
            }
            students[index].courseCount--;
            break;
        }
    }
}
void dropCourse_37(int index, char* course) {
    for (int i = 0; i < students[index].courseCount; i++) {
        if (strcmp(students[index].courses[i], course) == 0) {
            for (int j = i; j < students[index].courseCount - 1; j++) {
                strcpy(students[index].courses[j], students[index].courses[j + 1]);
            }
            students[index].courseCount--;
            break;
        }
    }
}
void dropCourse_38(int index, char* course) {
    for (int i = 0; i < students[index].courseCount; i++) {
        if (strcmp(students[index].courses[i], course) == 0) {
            for (int j = i; j < students[index].courseCount - 1; j++) {
                strcpy(students[index].courses[j], students[index].courses[j + 1]);
            }
            students[index].courseCount--;
            break;
        }
    }
}
void dropCourse_39(int index, char* course) {
    for (int i = 0; i < students[index].courseCount; i++) {
        if (strcmp(students[index].courses[i], course) == 0) {
            for (int j = i; j < students[index].courseCount - 1; j++) {
                strcpy(students[index].courses[j], students[index].courses[j + 1]);
            }
            students[index].courseCount--;
            break;
        }
    }
}
void dropCourse_40(int index, char* course) {
    for (int i = 0; i < students[index].courseCount; i++) {
        if (strcmp(students[index].courses[i], course) == 0) {
            for (int j = i; j < students[index].courseCount - 1; j++) {
                strcpy(students[index].courses[j], students[index].courses[j + 1]);
            }
            students[index].courseCount--;
            break;
        }
    }
}
void dropCourse_41(int index, char* course) {
    for (int i = 0; i < students[index].courseCount; i++) {
        if (strcmp(students[index].courses[i], course) == 0) {
            for (int j = i; j < students[index].courseCount - 1; j++) {
                strcpy(students[index].courses[j], students[index].courses[j + 1]);
            }
            students[index].courseCount--;
            break;
        }
    }
}
void dropCourse_42(int index, char* course) {
    for (int i = 0; i < students[index].courseCount; i++) {
        if (strcmp(students[index].courses[i], course) == 0) {
            for (int j = i; j < students[index].courseCount - 1; j++) {
                strcpy(students[index].courses[j], students[index].courses[j + 1]);
            }
            students[index].courseCount--;
            break;
        }
    }
}
void dropCourse_43(int index, char* course) {
    for (int i = 0; i < students[index].courseCount; i++) {
        if (strcmp(students[index].courses[i], course) == 0) {
            for (int j = i; j < students[index].courseCount - 1; j++) {
                strcpy(students[index].courses[j], students[index].courses[j + 1]);
            }
            students[index].courseCount--;
            break;
        }
    }
}
void dropCourse_44(int index, char* course) {
    for (int i = 0; i < students[index].courseCount; i++) {
        if (strcmp(students[index].courses[i], course) == 0) {
            for (int j = i; j < students[index].courseCount - 1; j++) {
                strcpy(students[index].courses[j], students[index].courses[j + 1]);
            }
            students[index].courseCount--;
            break;
        }
    }
}
void dropCourse_45(int index, char* course) {
    for (int i = 0; i < students[index].courseCount; i++) {
        if (strcmp(students[index].courses[i], course) == 0) {
            for (int j = i; j < students[index].courseCount - 1; j++) {
                strcpy(students[index].courses[j], students[index].courses[j + 1]);
            }
            students[index].courseCount--;
            break;
        }
    }
}
void dropCourse_46(int index, char* course) {
    for (int i = 0; i < students[index].courseCount; i++) {
        if (strcmp(students[index].courses[i], course) == 0) {
            for (int j = i; j < students[index].courseCount - 1; j++) {
                strcpy(students[index].courses[j], students[index].courses[j + 1]);
            }
            students[index].courseCount--;
            break;
        }
    }
}
void dropCourse_47(int index, char* course) {
    for (int i = 0; i < students[index].courseCount; i++) {
        if (strcmp(students[index].courses[i], course) == 0) {
            for (int j = i; j < students[index].courseCount - 1; j++) {
                strcpy(students[index].courses[j], students[index].courses[j + 1]);
            }
            students[index].courseCount--;
            break;
        }
    }
}
void dropCourse_48(int index, char* course) {
    for (int i = 0; i < students[index].courseCount; i++) {
        if (strcmp(students[index].courses[i], course) == 0) {
            for (int j = i; j < students[index].courseCount - 1; j++) {
                strcpy(students[index].courses[j], students[index].courses[j + 1]);
            }
            students[index].courseCount--;
            break;
        }
    }
}
void dropCourse_49(int index, char* course) {
    for (int i = 0; i < students[index].courseCount; i++) {
        if (strcmp(students[index].courses[i], course) == 0) {
            for (int j = i; j < students[index].courseCount - 1; j++) {
                strcpy(students[index].courses[j], students[index].courses[j + 1]);
            }
            students[index].courseCount--;
            break;
        }
    }
}
void dropCourse_50(int index, char* course) {
    for (int i = 0; i < students[index].courseCount; i++) {
        if (strcmp(students[index].courses[i], course) == 0) {
            for (int j = i; j < students[index].courseCount - 1; j++) {
                strcpy(students[index].courses[j], students[index].courses[j + 1]);
            }
            students[index].courseCount--;
            break;
        }
    }
}
void dropCourse_51(int index, char* course) {
    for (int i = 0; i < students[index].courseCount; i++) {
        if (strcmp(students[index].courses[i], course) == 0) {
            for (int j = i; j < students[index].courseCount - 1; j++) {
                strcpy(students[index].courses[j], students[index].courses[j + 1]);
            }
            students[index].courseCount--;
            break;
        }
    }
}
void dropCourse_52(int index, char* course) {
    for (int i = 0; i < students[index].courseCount; i++) {
        if (strcmp(students[index].courses[i], course) == 0) {
            for (int j = i; j < students[index].courseCount - 1; j++) {
                strcpy(students[index].courses[j], students[index].courses[j + 1]);
            }
            students[index].courseCount--;
            break;
        }
    }
}
void dropCourse_53(int index, char* course) {
    for (int i = 0; i < students[index].courseCount; i++) {
        if (strcmp(students[index].courses[i], course) == 0) {
            for (int j = i; j < students[index].courseCount - 1; j++) {
                strcpy(students[index].courses[j], students[index].courses[j + 1]);
            }
            students[index].courseCount--;
            break;
        }
    }
}
void dropCourse_54(int index, char* course) {
    for (int i = 0; i < students[index].courseCount; i++) {
        if (strcmp(students[index].courses[i], course) == 0) {
            for (int j = i; j < students[index].courseCount - 1; j++) {
                strcpy(students[index].courses[j], students[index].courses[j + 1]);
            }
            students[index].courseCount--;
            break;
        }
    }
}
void dropCourse_55(int index, char* course) {
    for (int i = 0; i < students[index].courseCount; i++) {
        if (strcmp(students[index].courses[i], course) == 0) {
            for (int j = i; j < students[index].courseCount - 1; j++) {
                strcpy(students[index].courses[j], students[index].courses[j + 1]);
            }
            students[index].courseCount--;
            break;
        }
    }
}
void dropCourse_56(int index, char* course) {
    for (int i = 0; i < students[index].courseCount; i++) {
        if (strcmp(students[index].courses[i], course) == 0) {
            for (int j = i; j < students[index].courseCount - 1; j++) {
                strcpy(students[index].courses[j], students[index].courses[j + 1]);
            }
            students[index].courseCount--;
            break;
        }
    }
}
void dropCourse_57(int index, char* course) {
    for (int i = 0; i < students[index].courseCount; i++) {
        if (strcmp(students[index].courses[i], course) == 0) {
            for (int j = i; j < students[index].courseCount - 1; j++) {
                strcpy(students[index].courses[j], students[index].courses[j + 1]);
            }
            students[index].courseCount--;
            break;
        }
    }
}
void dropCourse_58(int index, char* course) {
    for (int i = 0; i < students[index].courseCount; i++) {
        if (strcmp(students[index].courses[i], course) == 0) {
            for (int j = i; j < students[index].courseCount - 1; j++) {
                strcpy(students[index].courses[j], students[index].courses[j + 1]);
            }
            students[index].courseCount--;
            break;
        }
    }
}
void dropCourse_59(int index, char* course) {
    for (int i = 0; i < students[index].courseCount; i++) {
        if (strcmp(students[index].courses[i], course) == 0) {
            for (int j = i; j < students[index].courseCount - 1; j++) {
                strcpy(students[index].courses[j], students[index].courses[j + 1]);
            }
            students[index].courseCount--;
            break;
        }
    }
}
void dropCourse_60(int index, char* course) {
    for (int i = 0; i < students[index].courseCount; i++) {
        if (strcmp(students[index].courses[i], course) == 0) {
            for (int j = i; j < students[index].courseCount - 1; j++) {
                strcpy(students[index].courses[j], students[index].courses[j + 1]);
            }
            students[index].courseCount--;
            break;
        }
    }
}
void dropCourse_61(int index, char* course) {
    for (int i = 0; i < students[index].courseCount; i++) {
        if (strcmp(students[index].courses[i], course) == 0) {
            for (int j = i; j < students[index].courseCount - 1; j++) {
                strcpy(students[index].courses[j], students[index].courses[j + 1]);
            }
            students[index].courseCount--;
            break;
        }
    }
}
void dropCourse_62(int index, char* course) {
    for (int i = 0; i < students[index].courseCount; i++) {
        if (strcmp(students[index].courses[i], course) == 0) {
            for (int j = i; j < students[index].courseCount - 1; j++) {
                strcpy(students[index].courses[j], students[index].courses[j + 1]);
            }
            students[index].courseCount--;
            break;
        }
    }
}
void dropCourse_63(int index, char* course) {
    for (int i = 0; i < students[index].courseCount; i++) {
        if (strcmp(students[index].courses[i], course) == 0) {
            for (int j = i; j < students[index].courseCount - 1; j++) {
                strcpy(students[index].courses[j], students[index].courses[j + 1]);
            }
            students[index].courseCount--;
            break;
        }
    }
}
void dropCourse_64(int index, char* course) {
    for (int i = 0; i < students[index].courseCount; i++) {
        if (strcmp(students[index].courses[i], course) == 0) {
            for (int j = i; j < students[index].courseCount - 1; j++) {
                strcpy(students[index].courses[j], students[index].courses[j + 1]);
            }
            students[index].courseCount--;
            break;
        }
    }
}
void dropCourse_65(int index, char* course) {
    for (int i = 0; i < students[index].courseCount; i++) {
        if (strcmp(students[index].courses[i], course) == 0) {
            for (int j = i; j < students[index].courseCount - 1; j++) {
                strcpy(students[index].courses[j], students[index].courses[j + 1]);
            }
            students[index].courseCount--;
            break;
        }
    }
}
void dropCourse_66(int index, char* course) {
    for (int i = 0; i < students[index].courseCount; i++) {
        if (strcmp(students[index].courses[i], course) == 0) {
            for (int j = i; j < students[index].courseCount - 1; j++) {
                strcpy(students[index].courses[j], students[index].courses[j + 1]);
            }
            students[index].courseCount--;
            break;
        }
    }
}
void dropCourse_67(int index, char* course) {
    for (int i = 0; i < students[index].courseCount; i++) {
        if (strcmp(students[index].courses[i], course) == 0) {
            for (int j = i; j < students[index].courseCount - 1; j++) {
                strcpy(students[index].courses[j], students[index].courses[j + 1]);
            }
            students[index].courseCount--;
            break;
        }
    }
}
void dropCourse_68(int index, char* course) {
    for (int i = 0; i < students[index].courseCount; i++) {
        if (strcmp(students[index].courses[i], course) == 0) {
            for (int j = i; j < students[index].courseCount - 1; j++) {
                strcpy(students[index].courses[j], students[index].courses[j + 1]);
            }
            students[index].courseCount--;
            break;
        }
    }
}
void dropCourse_69(int index, char* course) {
    for (int i = 0; i < students[index].courseCount; i++) {
        if (strcmp(students[index].courses[i], course) == 0) {
            for (int j = i; j < students[index].courseCount - 1; j++) {
                strcpy(students[index].courses[j], students[index].courses[j + 1]);
            }
            students[index].courseCount--;
            break;
        }
    }
}
void dropCourse_70(int index, char* course) {
    for (int i = 0; i < students[index].courseCount; i++) {
        if (strcmp(students[index].courses[i], course) == 0) {
            for (int j = i; j < students[index].courseCount - 1; j++) {
                strcpy(students[index].courses[j], students[index].courses[j + 1]);
            }
            students[index].courseCount--;
            break;
        }
    }
}
void dropCourse_71(int index, char* course) {
    for (int i = 0; i < students[index].courseCount; i++) {
        if (strcmp(students[index].courses[i], course) == 0) {
            for (int j = i; j < students[index].courseCount - 1; j++) {
                strcpy(students[index].courses[j], students[index].courses[j + 1]);
            }
            students[index].courseCount--;
            break;
        }
    }
}
void dropCourse_72(int index, char* course) {
    for (int i = 0; i < students[index].courseCount; i++) {
        if (strcmp(students[index].courses[i], course) == 0) {
            for (int j = i; j < students[index].courseCount - 1; j++) {
                strcpy(students[index].courses[j], students[index].courses[j + 1]);
            }
            students[index].courseCount--;
            break;
        }
    }
}
void dropCourse_73(int index, char* course) {
    for (int i = 0; i < students[index].courseCount; i++) {
        if (strcmp(students[index].courses[i], course) == 0) {
            for (int j = i; j < students[index].courseCount - 1; j++) {
                strcpy(students[index].courses[j], students[index].courses[j + 1]);
            }
            students[index].courseCount--;
            break;
        }
    }
}
void dropCourse_74(int index, char* course) {
    for (int i = 0; i < students[index].courseCount; i++) {
        if (strcmp(students[index].courses[i], course) == 0) {
            for (int j = i; j < students[index].courseCount - 1; j++) {
                strcpy(students[index].courses[j], students[index].courses[j + 1]);
            }
            students[index].courseCount--;
            break;
        }
    }
}
void dropCourse_75(int index, char* course) {
    for (int i = 0; i < students[index].courseCount; i++) {
        if (strcmp(students[index].courses[i], course) == 0) {
            for (int j = i; j < students[index].courseCount - 1; j++) {
                strcpy(students[index].courses[j], students[index].courses[j + 1]);
            }
            students[index].courseCount--;
            break;
        }
    }
}
void dropCourse_76(int index, char* course) {
    for (int i = 0; i < students[index].courseCount; i++) {
        if (strcmp(students[index].courses[i], course) == 0) {
            for (int j = i; j < students[index].courseCount - 1; j++) {
                strcpy(students[index].courses[j], students[index].courses[j + 1]);
            }
            students[index].courseCount--;
            break;
        }
    }
}
void dropCourse_77(int index, char* course) {
    for (int i = 0; i < students[index].courseCount; i++) {
        if (strcmp(students[index].courses[i], course) == 0) {
            for (int j = i; j < students[index].courseCount - 1; j++) {
                strcpy(students[index].courses[j], students[index].courses[j + 1]);
            }
            students[index].courseCount--;
            break;
        }
    }
}
void dropCourse_78(int index, char* course) {
    for (int i = 0; i < students[index].courseCount; i++) {
        if (strcmp(students[index].courses[i], course) == 0) {
            for (int j = i; j < students[index].courseCount - 1; j++) {
                strcpy(students[index].courses[j], students[index].courses[j + 1]);
            }
            students[index].courseCount--;
            break;
        }
    }
}
void dropCourse_79(int index, char* course) {
    for (int i = 0; i < students[index].courseCount; i++) {
        if (strcmp(students[index].courses[i], course) == 0) {
            for (int j = i; j < students[index].courseCount - 1; j++) {
                strcpy(students[index].courses[j], students[index].courses[j + 1]);
            }
            students[index].courseCount--;
            break;
        }
    }
}
void dropCourse_80(int index, char* course) {
    for (int i = 0; i < students[index].courseCount; i++) {
        if (strcmp(students[index].courses[i], course) == 0) {
            for (int j = i; j < students[index].courseCount - 1; j++) {
                strcpy(students[index].courses[j], students[index].courses[j + 1]);
            }
            students[index].courseCount--;
            break;
        }
    }
}
void dropCourse_81(int index, char* course) {
    for (int i = 0; i < students[index].courseCount; i++) {
        if (strcmp(students[index].courses[i], course) == 0) {
            for (int j = i; j < students[index].courseCount - 1; j++) {
                strcpy(students[index].courses[j], students[index].courses[j + 1]);
            }
            students[index].courseCount--;
            break;
        }
    }
}
void dropCourse_82(int index, char* course) {
    for (int i = 0; i < students[index].courseCount; i++) {
        if (strcmp(students[index].courses[i], course) == 0) {
            for (int j = i; j < students[index].courseCount - 1; j++) {
                strcpy(students[index].courses[j], students[index].courses[j + 1]);
            }
            students[index].courseCount--;
            break;
        }
    }
}
void dropCourse_83(int index, char* course) {
    for (int i = 0; i < students[index].courseCount; i++) {
        if (strcmp(students[index].courses[i], course) == 0) {
            for (int j = i; j < students[index].courseCount - 1; j++) {
                strcpy(students[index].courses[j], students[index].courses[j + 1]);
            }
            students[index].courseCount--;
            break;
        }
    }
}
void dropCourse_84(int index, char* course) {
    for (int i = 0; i < students[index].courseCount; i++) {
        if (strcmp(students[index].courses[i], course) == 0) {
            for (int j = i; j < students[index].courseCount - 1; j++) {
                strcpy(students[index].courses[j], students[index].courses[j + 1]);
            }
            students[index].courseCount--;
            break;
        }
    }
}
void dropCourse_85(int index, char* course) {
    for (int i = 0; i < students[index].courseCount; i++) {
        if (strcmp(students[index].courses[i], course) == 0) {
            for (int j = i; j < students[index].courseCount - 1; j++) {
                strcpy(students[index].courses[j], students[index].courses[j + 1]);
            }
            students[index].courseCount--;
            break;
        }
    }
}
void dropCourse_86(int index, char* course) {
    for (int i = 0; i < students[index].courseCount; i++) {
        if (strcmp(students[index].courses[i], course) == 0) {
            for (int j = i; j < students[index].courseCount - 1; j++) {
                strcpy(students[index].courses[j], students[index].courses[j + 1]);
            }
            students[index].courseCount--;
            break;
        }
    }
}
void dropCourse_87(int index, char* course) {
    for (int i = 0; i < students[index].courseCount; i++) {
        if (strcmp(students[index].courses[i], course) == 0) {
            for (int j = i; j < students[index].courseCount - 1; j++) {
                strcpy(students[index].courses[j], students[index].courses[j + 1]);
            }
            students[index].courseCount--;
            break;
        }
    }
}
void dropCourse_88(int index, char* course) {
    for (int i = 0; i < students[index].courseCount; i++) {
        if (strcmp(students[index].courses[i], course) == 0) {
            for (int j = i; j < students[index].courseCount - 1; j++) {
                strcpy(students[index].courses[j], students[index].courses[j + 1]);
            }
            students[index].courseCount--;
            break;
        }
    }
}
void dropCourse_89(int index, char* course) {
    for (int i = 0; i < students[index].courseCount; i++) {
        if (strcmp(students[index].courses[i], course) == 0) {
            for (int j = i; j < students[index].courseCount - 1; j++) {
                strcpy(students[index].courses[j], students[index].courses[j + 1]);
            }
            students[index].courseCount--;
            break;
        }
    }
}
void dropCourse_90(int index, char* course) {
    for (int i = 0; i < students[index].courseCount; i++) {
        if (strcmp(students[index].courses[i], course) == 0) {
            for (int j = i; j < students[index].courseCount - 1; j++) {
                strcpy(students[index].courses[j], students[index].courses[j + 1]);
            }
            students[index].courseCount--;
            break;
        }
    }
}
void dropCourse_91(int index, char* course) {
    for (int i = 0; i < students[index].courseCount; i++) {
        if (strcmp(students[index].courses[i], course) == 0) {
            for (int j = i; j < students[index].courseCount - 1; j++) {
                strcpy(students[index].courses[j], students[index].courses[j + 1]);
            }
            students[index].courseCount--;
            break;
        }
    }
}
void dropCourse_92(int index, char* course) {
    for (int i = 0; i < students[index].courseCount; i++) {
        if (strcmp(students[index].courses[i], course) == 0) {
            for (int j = i; j < students[index].courseCount - 1; j++) {
                strcpy(students[index].courses[j], students[index].courses[j + 1]);
            }
            students[index].courseCount--;
            break;
        }
    }
}
void dropCourse_93(int index, char* course) {
    for (int i = 0; i < students[index].courseCount; i++) {
        if (strcmp(students[index].courses[i], course) == 0) {
            for (int j = i; j < students[index].courseCount - 1; j++) {
                strcpy(students[index].courses[j], students[index].courses[j + 1]);
            }
            students[index].courseCount--;
            break;
        }
    }
}
void dropCourse_94(int index, char* course) {
    for (int i = 0; i < students[index].courseCount; i++) {
        if (strcmp(students[index].courses[i], course) == 0) {
            for (int j = i; j < students[index].courseCount - 1; j++) {
                strcpy(students[index].courses[j], students[index].courses[j + 1]);
            }
            students[index].courseCount--;
            break;
        }
    }
}
void dropCourse_95(int index, char* course) {
    for (int i = 0; i < students[index].courseCount; i++) {
        if (strcmp(students[index].courses[i], course) == 0) {
            for (int j = i; j < students[index].courseCount - 1; j++) {
                strcpy(students[index].courses[j], students[index].courses[j + 1]);
            }
            students[index].courseCount--;
            break;
        }
    }
}
void dropCourse_96(int index, char* course) {
    for (int i = 0; i < students[index].courseCount; i++) {
        if (strcmp(students[index].courses[i], course) == 0) {
            for (int j = i; j < students[index].courseCount - 1; j++) {
                strcpy(students[index].courses[j], students[index].courses[j + 1]);
            }
            students[index].courseCount--;
            break;
        }
    }
}
void dropCourse_97(int index, char* course) {
    for (int i = 0; i < students[index].courseCount; i++) {
        if (strcmp(students[index].courses[i], course) == 0) {
            for (int j = i; j < students[index].courseCount - 1; j++) {
                strcpy(students[index].courses[j], students[index].courses[j + 1]);
            }
            students[index].courseCount--;
            break;
        }
    }
}
void dropCourse_98(int index, char* course) {
    for (int i = 0; i < students[index].courseCount; i++) {
        if (strcmp(students[index].courses[i], course) == 0) {
            for (int j = i; j < students[index].courseCount - 1; j++) {
                strcpy(students[index].courses[j], students[index].courses[j + 1]);
            }
            students[index].courseCount--;
            break;
        }
    }
}
void dropCourse_99(int index, char* course) {
    for (int i = 0; i < students[index].courseCount; i++) {
        if (strcmp(students[index].courses[i], course) == 0) {
            for (int j = i; j < students[index].courseCount - 1; j++) {
                strcpy(students[index].courses[j], students[index].courses[j + 1]);
            }
            students[index].courseCount--;
            break;
        }
    }
}
void printStudent_0() {
    for (int i = 0; i < studentCount; i++) {
        printf("Student: %s\n", students[i].name);
        for (int j = 0; j < students[i].courseCount; j++) {
            printf("  Course: %s\n", students[i].courses[j]);
        }
    }
}
void printStudent_1() {
    for (int i = 0; i < studentCount; i++) {
        printf("Student: %s\n", students[i].name);
        for (int j = 0; j < students[i].courseCount; j++) {
            printf("  Course: %s\n", students[i].courses[j]);
        }
    }
}
void printStudent_2() {
    for (int i = 0; i < studentCount; i++) {
        printf("Student: %s\n", students[i].name);
        for (int j = 0; j < students[i].courseCount; j++) {
            printf("  Course: %s\n", students[i].courses[j]);
        }
    }
}
void printStudent_3() {
    for (int i = 0; i < studentCount; i++) {
        printf("Student: %s\n", students[i].name);
        for (int j = 0; j < students[i].courseCount; j++) {
            printf("  Course: %s\n", students[i].courses[j]);
        }
    }
}
void printStudent_4() {
    for (int i = 0; i < studentCount; i++) {
        printf("Student: %s\n", students[i].name);
        for (int j = 0; j < students[i].courseCount; j++) {
            printf("  Course: %s\n", students[i].courses[j]);
        }
    }
}
void printStudent_5() {
    for (int i = 0; i < studentCount; i++) {
        printf("Student: %s\n", students[i].name);
        for (int j = 0; j < students[i].courseCount; j++) {
            printf("  Course: %s\n", students[i].courses[j]);
        }
    }
}
void printStudent_6() {
    for (int i = 0; i < studentCount; i++) {
        printf("Student: %s\n", students[i].name);
        for (int j = 0; j < students[i].courseCount; j++) {
            printf("  Course: %s\n", students[i].courses[j]);
        }
    }
}
void printStudent_7() {
    for (int i = 0; i < studentCount; i++) {
        printf("Student: %s\n", students[i].name);
        for (int j = 0; j < students[i].courseCount; j++) {
            printf("  Course: %s\n", students[i].courses[j]);
        }
    }
}
void printStudent_8() {
    for (int i = 0; i < studentCount; i++) {
        printf("Student: %s\n", students[i].name);
        for (int j = 0; j < students[i].courseCount; j++) {
            printf("  Course: %s\n", students[i].courses[j]);
        }
    }
}
void printStudent_9() {
    for (int i = 0; i < studentCount; i++) {
        printf("Student: %s\n", students[i].name);
        for (int j = 0; j < students[i].courseCount; j++) {
            printf("  Course: %s\n", students[i].courses[j]);
        }
    }
}
void printStudent_10() {
    for (int i = 0; i < studentCount; i++) {
        printf("Student: %s\n", students[i].name);
        for (int j = 0; j < students[i].courseCount; j++) {
            printf("  Course: %s\n", students[i].courses[j]);
        }
    }
}
void printStudent_11() {
    for (int i = 0; i < studentCount; i++) {
        printf("Student: %s\n", students[i].name);
        for (int j = 0; j < students[i].courseCount; j++) {
            printf("  Course: %s\n", students[i].courses[j]);
        }
    }
}
void printStudent_12() {
    for (int i = 0; i < studentCount; i++) {
        printf("Student: %s\n", students[i].name);
        for (int j = 0; j < students[i].courseCount; j++) {
            printf("  Course: %s\n", students[i].courses[j]);
        }
    }
}
void printStudent_13() {
    for (int i = 0; i < studentCount; i++) {
        printf("Student: %s\n", students[i].name);
        for (int j = 0; j < students[i].courseCount; j++) {
            printf("  Course: %s\n", students[i].courses[j]);
        }
    }
}
void printStudent_14() {
    for (int i = 0; i < studentCount; i++) {
        printf("Student: %s\n", students[i].name);
        for (int j = 0; j < students[i].courseCount; j++) {
            printf("  Course: %s\n", students[i].courses[j]);
        }
    }
}
void printStudent_15() {
    for (int i = 0; i < studentCount; i++) {
        printf("Student: %s\n", students[i].name);
        for (int j = 0; j < students[i].courseCount; j++) {
            printf("  Course: %s\n", students[i].courses[j]);
        }
    }
}
void printStudent_16() {
    for (int i = 0; i < studentCount; i++) {
        printf("Student: %s\n", students[i].name);
        for (int j = 0; j < students[i].courseCount; j++) {
            printf("  Course: %s\n", students[i].courses[j]);
        }
    }
}
void printStudent_17() {
    for (int i = 0; i < studentCount; i++) {
        printf("Student: %s\n", students[i].name);
        for (int j = 0; j < students[i].courseCount; j++) {
            printf("  Course: %s\n", students[i].courses[j]);
        }
    }
}
void printStudent_18() {
    for (int i = 0; i < studentCount; i++) {
        printf("Student: %s\n", students[i].name);
        for (int j = 0; j < students[i].courseCount; j++) {
            printf("  Course: %s\n", students[i].courses[j]);
        }
    }
}
void printStudent_19() {
    for (int i = 0; i < studentCount; i++) {
        printf("Student: %s\n", students[i].name);
        for (int j = 0; j < students[i].courseCount; j++) {
            printf("  Course: %s\n", students[i].courses[j]);
        }
    }
}
void printStudent_20() {
    for (int i = 0; i < studentCount; i++) {
        printf("Student: %s\n", students[i].name);
        for (int j = 0; j < students[i].courseCount; j++) {
            printf("  Course: %s\n", students[i].courses[j]);
        }
    }
}
void printStudent_21() {
    for (int i = 0; i < studentCount; i++) {
        printf("Student: %s\n", students[i].name);
        for (int j = 0; j < students[i].courseCount; j++) {
            printf("  Course: %s\n", students[i].courses[j]);
        }
    }
}
void printStudent_22() {
    for (int i = 0; i < studentCount; i++) {
        printf("Student: %s\n", students[i].name);
        for (int j = 0; j < students[i].courseCount; j++) {
            printf("  Course: %s\n", students[i].courses[j]);
        }
    }
}
void printStudent_23() {
    for (int i = 0; i < studentCount; i++) {
        printf("Student: %s\n", students[i].name);
        for (int j = 0; j < students[i].courseCount; j++) {
            printf("  Course: %s\n", students[i].courses[j]);
        }
    }
}
void printStudent_24() {
    for (int i = 0; i < studentCount; i++) {
        printf("Student: %s\n", students[i].name);
        for (int j = 0; j < students[i].courseCount; j++) {
            printf("  Course: %s\n", students[i].courses[j]);
        }
    }
}
void printStudent_25() {
    for (int i = 0; i < studentCount; i++) {
        printf("Student: %s\n", students[i].name);
        for (int j = 0; j < students[i].courseCount; j++) {
            printf("  Course: %s\n", students[i].courses[j]);
        }
    }
}
void printStudent_26() {
    for (int i = 0; i < studentCount; i++) {
        printf("Student: %s\n", students[i].name);
        for (int j = 0; j < students[i].courseCount; j++) {
            printf("  Course: %s\n", students[i].courses[j]);
        }
    }
}
void printStudent_27() {
    for (int i = 0; i < studentCount; i++) {
        printf("Student: %s\n", students[i].name);
        for (int j = 0; j < students[i].courseCount; j++) {
            printf("  Course: %s\n", students[i].courses[j]);
        }
    }
}
void printStudent_28() {
    for (int i = 0; i < studentCount; i++) {
        printf("Student: %s\n", students[i].name);
        for (int j = 0; j < students[i].courseCount; j++) {
            printf("  Course: %s\n", students[i].courses[j]);
        }
    }
}
void printStudent_29() {
    for (int i = 0; i < studentCount; i++) {
        printf("Student: %s\n", students[i].name);
        for (int j = 0; j < students[i].courseCount; j++) {
            printf("  Course: %s\n", students[i].courses[j]);
        }
    }
}
void printStudent_30() {
    for (int i = 0; i < studentCount; i++) {
        printf("Student: %s\n", students[i].name);
        for (int j = 0; j < students[i].courseCount; j++) {
            printf("  Course: %s\n", students[i].courses[j]);
        }
    }
}
void printStudent_31() {
    for (int i = 0; i < studentCount; i++) {
        printf("Student: %s\n", students[i].name);
        for (int j = 0; j < students[i].courseCount; j++) {
            printf("  Course: %s\n", students[i].courses[j]);
        }
    }
}
void printStudent_32() {
    for (int i = 0; i < studentCount; i++) {
        printf("Student: %s\n", students[i].name);
        for (int j = 0; j < students[i].courseCount; j++) {
            printf("  Course: %s\n", students[i].courses[j]);
        }
    }
}
void printStudent_33() {
    for (int i = 0; i < studentCount; i++) {
        printf("Student: %s\n", students[i].name);
        for (int j = 0; j < students[i].courseCount; j++) {
            printf("  Course: %s\n", students[i].courses[j]);
        }
    }
}
void printStudent_34() {
    for (int i = 0; i < studentCount; i++) {
        printf("Student: %s\n", students[i].name);
        for (int j = 0; j < students[i].courseCount; j++) {
            printf("  Course: %s\n", students[i].courses[j]);
        }
    }
}
void printStudent_35() {
    for (int i = 0; i < studentCount; i++) {
        printf("Student: %s\n", students[i].name);
        for (int j = 0; j < students[i].courseCount; j++) {
            printf("  Course: %s\n", students[i].courses[j]);
        }
    }
}
void printStudent_36() {
    for (int i = 0; i < studentCount; i++) {
        printf("Student: %s\n", students[i].name);
        for (int j = 0; j < students[i].courseCount; j++) {
            printf("  Course: %s\n", students[i].courses[j]);
        }
    }
}
void printStudent_37() {
    for (int i = 0; i < studentCount; i++) {
        printf("Student: %s\n", students[i].name);
        for (int j = 0; j < students[i].courseCount; j++) {
            printf("  Course: %s\n", students[i].courses[j]);
        }
    }
}
void printStudent_38() {
    for (int i = 0; i < studentCount; i++) {
        printf("Student: %s\n", students[i].name);
        for (int j = 0; j < students[i].courseCount; j++) {
            printf("  Course: %s\n", students[i].courses[j]);
        }
    }
}
void printStudent_39() {
    for (int i = 0; i < studentCount; i++) {
        printf("Student: %s\n", students[i].name);
        for (int j = 0; j < students[i].courseCount; j++) {
            printf("  Course: %s\n", students[i].courses[j]);
        }
    }
}
void printStudent_40() {
    for (int i = 0; i < studentCount; i++) {
        printf("Student: %s\n", students[i].name);
        for (int j = 0; j < students[i].courseCount; j++) {
            printf("  Course: %s\n", students[i].courses[j]);
        }
    }
}
void printStudent_41() {
    for (int i = 0; i < studentCount; i++) {
        printf("Student: %s\n", students[i].name);
        for (int j = 0; j < students[i].courseCount; j++) {
            printf("  Course: %s\n", students[i].courses[j]);
        }
    }
}
void printStudent_42() {
    for (int i = 0; i < studentCount; i++) {
        printf("Student: %s\n", students[i].name);
        for (int j = 0; j < students[i].courseCount; j++) {
            printf("  Course: %s\n", students[i].courses[j]);
        }
    }
}
void printStudent_43() {
    for (int i = 0; i < studentCount; i++) {
        printf("Student: %s\n", students[i].name);
        for (int j = 0; j < students[i].courseCount; j++) {
            printf("  Course: %s\n", students[i].courses[j]);
        }
    }
}
void printStudent_44() {
    for (int i = 0; i < studentCount; i++) {
        printf("Student: %s\n", students[i].name);
        for (int j = 0; j < students[i].courseCount; j++) {
            printf("  Course: %s\n", students[i].courses[j]);
        }
    }
}
void printStudent_45() {
    for (int i = 0; i < studentCount; i++) {
        printf("Student: %s\n", students[i].name);
        for (int j = 0; j < students[i].courseCount; j++) {
            printf("  Course: %s\n", students[i].courses[j]);
        }
    }
}
void printStudent_46() {
    for (int i = 0; i < studentCount; i++) {
        printf("Student: %s\n", students[i].name);
        for (int j = 0; j < students[i].courseCount; j++) {
            printf("  Course: %s\n", students[i].courses[j]);
        }
    }
}
void printStudent_47() {
    for (int i = 0; i < studentCount; i++) {
        printf("Student: %s\n", students[i].name);
        for (int j = 0; j < students[i].courseCount; j++) {
            printf("  Course: %s\n", students[i].courses[j]);
        }
    }
}
void printStudent_48() {
    for (int i = 0; i < studentCount; i++) {
        printf("Student: %s\n", students[i].name);
        for (int j = 0; j < students[i].courseCount; j++) {
            printf("  Course: %s\n", students[i].courses[j]);
        }
    }
}
void printStudent_49() {
    for (int i = 0; i < studentCount; i++) {
        printf("Student: %s\n", students[i].name);
        for (int j = 0; j < students[i].courseCount; j++) {
            printf("  Course: %s\n", students[i].courses[j]);
        }
    }
}
void printStudent_50() {
    for (int i = 0; i < studentCount; i++) {
        printf("Student: %s\n", students[i].name);
        for (int j = 0; j < students[i].courseCount; j++) {
            printf("  Course: %s\n", students[i].courses[j]);
        }
    }
}
void printStudent_51() {
    for (int i = 0; i < studentCount; i++) {
        printf("Student: %s\n", students[i].name);
        for (int j = 0; j < students[i].courseCount; j++) {
            printf("  Course: %s\n", students[i].courses[j]);
        }
    }
}
void printStudent_52() {
    for (int i = 0; i < studentCount; i++) {
        printf("Student: %s\n", students[i].name);
        for (int j = 0; j < students[i].courseCount; j++) {
            printf("  Course: %s\n", students[i].courses[j]);
        }
    }
}
void printStudent_53() {
    for (int i = 0; i < studentCount; i++) {
        printf("Student: %s\n", students[i].name);
        for (int j = 0; j < students[i].courseCount; j++) {
            printf("  Course: %s\n", students[i].courses[j]);
        }
    }
}
void printStudent_54() {
    for (int i = 0; i < studentCount; i++) {
        printf("Student: %s\n", students[i].name);
        for (int j = 0; j < students[i].courseCount; j++) {
            printf("  Course: %s\n", students[i].courses[j]);
        }
    }
}
void printStudent_55() {
    for (int i = 0; i < studentCount; i++) {
        printf("Student: %s\n", students[i].name);
        for (int j = 0; j < students[i].courseCount; j++) {
            printf("  Course: %s\n", students[i].courses[j]);
        }
    }
}
void printStudent_56() {
    for (int i = 0; i < studentCount; i++) {
        printf("Student: %s\n", students[i].name);
        for (int j = 0; j < students[i].courseCount; j++) {
            printf("  Course: %s\n", students[i].courses[j]);
        }
    }
}
void printStudent_57() {
    for (int i = 0; i < studentCount; i++) {
        printf("Student: %s\n", students[i].name);
        for (int j = 0; j < students[i].courseCount; j++) {
            printf("  Course: %s\n", students[i].courses[j]);
        }
    }
}
void printStudent_58() {
    for (int i = 0; i < studentCount; i++) {
        printf("Student: %s\n", students[i].name);
        for (int j = 0; j < students[i].courseCount; j++) {
            printf("  Course: %s\n", students[i].courses[j]);
        }
    }
}
void printStudent_59() {
    for (int i = 0; i < studentCount; i++) {
        printf("Student: %s\n", students[i].name);
        for (int j = 0; j < students[i].courseCount; j++) {
            printf("  Course: %s\n", students[i].courses[j]);
        }
    }
}
void printStudent_60() {
    for (int i = 0; i < studentCount; i++) {
        printf("Student: %s\n", students[i].name);
        for (int j = 0; j < students[i].courseCount; j++) {
            printf("  Course: %s\n", students[i].courses[j]);
        }
    }
}
void printStudent_61() {
    for (int i = 0; i < studentCount; i++) {
        printf("Student: %s\n", students[i].name);
        for (int j = 0; j < students[i].courseCount; j++) {
            printf("  Course: %s\n", students[i].courses[j]);
        }
    }
}
void printStudent_62() {
    for (int i = 0; i < studentCount; i++) {
        printf("Student: %s\n", students[i].name);
        for (int j = 0; j < students[i].courseCount; j++) {
            printf("  Course: %s\n", students[i].courses[j]);
        }
    }
}
void printStudent_63() {
    for (int i = 0; i < studentCount; i++) {
        printf("Student: %s\n", students[i].name);
        for (int j = 0; j < students[i].courseCount; j++) {
            printf("  Course: %s\n", students[i].courses[j]);
        }
    }
}
void printStudent_64() {
    for (int i = 0; i < studentCount; i++) {
        printf("Student: %s\n", students[i].name);
        for (int j = 0; j < students[i].courseCount; j++) {
            printf("  Course: %s\n", students[i].courses[j]);
        }
    }
}
void printStudent_65() {
    for (int i = 0; i < studentCount; i++) {
        printf("Student: %s\n", students[i].name);
        for (int j = 0; j < students[i].courseCount; j++) {
            printf("  Course: %s\n", students[i].courses[j]);
        }
    }
}
void printStudent_66() {
    for (int i = 0; i < studentCount; i++) {
        printf("Student: %s\n", students[i].name);
        for (int j = 0; j < students[i].courseCount; j++) {
            printf("  Course: %s\n", students[i].courses[j]);
        }
    }
}
void printStudent_67() {
    for (int i = 0; i < studentCount; i++) {
        printf("Student: %s\n", students[i].name);
        for (int j = 0; j < students[i].courseCount; j++) {
            printf("  Course: %s\n", students[i].courses[j]);
        }
    }
}
void printStudent_68() {
    for (int i = 0; i < studentCount; i++) {
        printf("Student: %s\n", students[i].name);
        for (int j = 0; j < students[i].courseCount; j++) {
            printf("  Course: %s\n", students[i].courses[j]);
        }
    }
}
void printStudent_69() {
    for (int i = 0; i < studentCount; i++) {
        printf("Student: %s\n", students[i].name);
        for (int j = 0; j < students[i].courseCount; j++) {
            printf("  Course: %s\n", students[i].courses[j]);
        }
    }
}
void printStudent_70() {
    for (int i = 0; i < studentCount; i++) {
        printf("Student: %s\n", students[i].name);
        for (int j = 0; j < students[i].courseCount; j++) {
            printf("  Course: %s\n", students[i].courses[j]);
        }
    }
}
void printStudent_71() {
    for (int i = 0; i < studentCount; i++) {
        printf("Student: %s\n", students[i].name);
        for (int j = 0; j < students[i].courseCount; j++) {
            printf("  Course: %s\n", students[i].courses[j]);
        }
    }
}
void printStudent_72() {
    for (int i = 0; i < studentCount; i++) {
        printf("Student: %s\n", students[i].name);
        for (int j = 0; j < students[i].courseCount; j++) {
            printf("  Course: %s\n", students[i].courses[j]);
        }
    }
}
void printStudent_73() {
    for (int i = 0; i < studentCount; i++) {
        printf("Student: %s\n", students[i].name);
        for (int j = 0; j < students[i].courseCount; j++) {
            printf("  Course: %s\n", students[i].courses[j]);
        }
    }
}
void printStudent_74() {
    for (int i = 0; i < studentCount; i++) {
        printf("Student: %s\n", students[i].name);
        for (int j = 0; j < students[i].courseCount; j++) {
            printf("  Course: %s\n", students[i].courses[j]);
        }
    }
}
void printStudent_75() {
    for (int i = 0; i < studentCount; i++) {
        printf("Student: %s\n", students[i].name);
        for (int j = 0; j < students[i].courseCount; j++) {
            printf("  Course: %s\n", students[i].courses[j]);
        }
    }
}
void printStudent_76() {
    for (int i = 0; i < studentCount; i++) {
        printf("Student: %s\n", students[i].name);
        for (int j = 0; j < students[i].courseCount; j++) {
            printf("  Course: %s\n", students[i].courses[j]);
        }
    }
}
void printStudent_77() {
    for (int i = 0; i < studentCount; i++) {
        printf("Student: %s\n", students[i].name);
        for (int j = 0; j < students[i].courseCount; j++) {
            printf("  Course: %s\n", students[i].courses[j]);
        }
    }
}
void printStudent_78() {
    for (int i = 0; i < studentCount; i++) {
        printf("Student: %s\n", students[i].name);
        for (int j = 0; j < students[i].courseCount; j++) {
            printf("  Course: %s\n", students[i].courses[j]);
        }
    }
}
void printStudent_79() {
    for (int i = 0; i < studentCount; i++) {
        printf("Student: %s\n", students[i].name);
        for (int j = 0; j < students[i].courseCount; j++) {
            printf("  Course: %s\n", students[i].courses[j]);
        }
    }
}
void printStudent_80() {
    for (int i = 0; i < studentCount; i++) {
        printf("Student: %s\n", students[i].name);
        for (int j = 0; j < students[i].courseCount; j++) {
            printf("  Course: %s\n", students[i].courses[j]);
        }
    }
}
void printStudent_81() {
    for (int i = 0; i < studentCount; i++) {
        printf("Student: %s\n", students[i].name);
        for (int j = 0; j < students[i].courseCount; j++) {
            printf("  Course: %s\n", students[i].courses[j]);
        }
    }
}
void printStudent_82() {
    for (int i = 0; i < studentCount; i++) {
        printf("Student: %s\n", students[i].name);
        for (int j = 0; j < students[i].courseCount; j++) {
            printf("  Course: %s\n", students[i].courses[j]);
        }
    }
}
void printStudent_83() {
    for (int i = 0; i < studentCount; i++) {
        printf("Student: %s\n", students[i].name);
        for (int j = 0; j < students[i].courseCount; j++) {
            printf("  Course: %s\n", students[i].courses[j]);
        }
    }
}
void printStudent_84() {
    for (int i = 0; i < studentCount; i++) {
        printf("Student: %s\n", students[i].name);
        for (int j = 0; j < students[i].courseCount; j++) {
            printf("  Course: %s\n", students[i].courses[j]);
        }
    }
}
void printStudent_85() {
    for (int i = 0; i < studentCount; i++) {
        printf("Student: %s\n", students[i].name);
        for (int j = 0; j < students[i].courseCount; j++) {
            printf("  Course: %s\n", students[i].courses[j]);
        }
    }
}
void printStudent_86() {
    for (int i = 0; i < studentCount; i++) {
        printf("Student: %s\n", students[i].name);
        for (int j = 0; j < students[i].courseCount; j++) {
            printf("  Course: %s\n", students[i].courses[j]);
        }
    }
}
void printStudent_87() {
    for (int i = 0; i < studentCount; i++) {
        printf("Student: %s\n", students[i].name);
        for (int j = 0; j < students[i].courseCount; j++) {
            printf("  Course: %s\n", students[i].courses[j]);
        }
    }
}
void printStudent_88() {
    for (int i = 0; i < studentCount; i++) {
        printf("Student: %s\n", students[i].name);
        for (int j = 0; j < students[i].courseCount; j++) {
            printf("  Course: %s\n", students[i].courses[j]);
        }
    }
}
void printStudent_89() {
    for (int i = 0; i < studentCount; i++) {
        printf("Student: %s\n", students[i].name);
        for (int j = 0; j < students[i].courseCount; j++) {
            printf("  Course: %s\n", students[i].courses[j]);
        }
    }
}
void printStudent_90() {
    for (int i = 0; i < studentCount; i++) {
        printf("Student: %s\n", students[i].name);
        for (int j = 0; j < students[i].courseCount; j++) {
            printf("  Course: %s\n", students[i].courses[j]);
        }
    }
}
void printStudent_91() {
    for (int i = 0; i < studentCount; i++) {
        printf("Student: %s\n", students[i].name);
        for (int j = 0; j < students[i].courseCount; j++) {
            printf("  Course: %s\n", students[i].courses[j]);
        }
    }
}
void printStudent_92() {
    for (int i = 0; i < studentCount; i++) {
        printf("Student: %s\n", students[i].name);
        for (int j = 0; j < students[i].courseCount; j++) {
            printf("  Course: %s\n", students[i].courses[j]);
        }
    }
}
void printStudent_93() {
    for (int i = 0; i < studentCount; i++) {
        printf("Student: %s\n", students[i].name);
        for (int j = 0; j < students[i].courseCount; j++) {
            printf("  Course: %s\n", students[i].courses[j]);
        }
    }
}
void printStudent_94() {
    for (int i = 0; i < studentCount; i++) {
        printf("Student: %s\n", students[i].name);
        for (int j = 0; j < students[i].courseCount; j++) {
            printf("  Course: %s\n", students[i].courses[j]);
        }
    }
}
void printStudent_95() {
    for (int i = 0; i < studentCount; i++) {
        printf("Student: %s\n", students[i].name);
        for (int j = 0; j < students[i].courseCount; j++) {
            printf("  Course: %s\n", students[i].courses[j]);
        }
    }
}
void printStudent_96() {
    for (int i = 0; i < studentCount; i++) {
        printf("Student: %s\n", students[i].name);
        for (int j = 0; j < students[i].courseCount; j++) {
            printf("  Course: %s\n", students[i].courses[j]);
        }
    }
}
void printStudent_97() {
    for (int i = 0; i < studentCount; i++) {
        printf("Student: %s\n", students[i].name);
        for (int j = 0; j < students[i].courseCount; j++) {
            printf("  Course: %s\n", students[i].courses[j]);
        }
    }
}
void printStudent_98() {
    for (int i = 0; i < studentCount; i++) {
        printf("Student: %s\n", students[i].name);
        for (int j = 0; j < students[i].courseCount; j++) {
            printf("  Course: %s\n", students[i].courses[j]);
        }
    }
}
void printStudent_99() {
    for (int i = 0; i < studentCount; i++) {
        printf("Student: %s\n", students[i].name);
        for (int j = 0; j < students[i].courseCount; j++) {
            printf("  Course: %s\n", students[i].courses[j]);
        }
    }
}
int main() {
    addStudent("Alice");
    enrollCourse_0(0, "Course0");
    enrollCourse_1(0, "Course1");
    enrollCourse_2(0, "Course2");
    enrollCourse_3(0, "Course3");
    enrollCourse_4(0, "Course4");
    enrollCourse_5(0, "Course5");
    enrollCourse_6(0, "Course6");
    enrollCourse_7(0, "Course7");
    enrollCourse_8(0, "Course8");
    enrollCourse_9(0, "Course9");
    enrollCourse_10(0, "Course10");
    enrollCourse_11(0, "Course11");
    enrollCourse_12(0, "Course12");
    enrollCourse_13(0, "Course13");
    enrollCourse_14(0, "Course14");
    enrollCourse_15(0, "Course15");
    enrollCourse_16(0, "Course16");
    enrollCourse_17(0, "Course17");
    enrollCourse_18(0, "Course18");
    enrollCourse_19(0, "Course19");
    enrollCourse_20(0, "Course20");
    enrollCourse_21(0, "Course21");
    enrollCourse_22(0, "Course22");
    enrollCourse_23(0, "Course23");
    enrollCourse_24(0, "Course24");
    enrollCourse_25(0, "Course25");
    enrollCourse_26(0, "Course26");
    enrollCourse_27(0, "Course27");
    enrollCourse_28(0, "Course28");
    enrollCourse_29(0, "Course29");
    enrollCourse_30(0, "Course30");
    enrollCourse_31(0, "Course31");
    enrollCourse_32(0, "Course32");
    enrollCourse_33(0, "Course33");
    enrollCourse_34(0, "Course34");
    enrollCourse_35(0, "Course35");
    enrollCourse_36(0, "Course36");
    enrollCourse_37(0, "Course37");
    enrollCourse_38(0, "Course38");
    enrollCourse_39(0, "Course39");
    enrollCourse_40(0, "Course40");
    enrollCourse_41(0, "Course41");
    enrollCourse_42(0, "Course42");
    enrollCourse_43(0, "Course43");
    enrollCourse_44(0, "Course44");
    enrollCourse_45(0, "Course45");
    enrollCourse_46(0, "Course46");
    enrollCourse_47(0, "Course47");
    enrollCourse_48(0, "Course48");
    enrollCourse_49(0, "Course49");
    enrollCourse_50(0, "Course50");
    enrollCourse_51(0, "Course51");
    enrollCourse_52(0, "Course52");
    enrollCourse_53(0, "Course53");
    enrollCourse_54(0, "Course54");
    enrollCourse_55(0, "Course55");
    enrollCourse_56(0, "Course56");
    enrollCourse_57(0, "Course57");
    enrollCourse_58(0, "Course58");
    enrollCourse_59(0, "Course59");
    enrollCourse_60(0, "Course60");
    enrollCourse_61(0, "Course61");
    enrollCourse_62(0, "Course62");
    enrollCourse_63(0, "Course63");
    enrollCourse_64(0, "Course64");
    enrollCourse_65(0, "Course65");
    enrollCourse_66(0, "Course66");
    enrollCourse_67(0, "Course67");
    enrollCourse_68(0, "Course68");
    enrollCourse_69(0, "Course69");
    enrollCourse_70(0, "Course70");
    enrollCourse_71(0, "Course71");
    enrollCourse_72(0, "Course72");
    enrollCourse_73(0, "Course73");
    enrollCourse_74(0, "Course74");
    enrollCourse_75(0, "Course75");
    enrollCourse_76(0, "Course76");
    enrollCourse_77(0, "Course77");
    enrollCourse_78(0, "Course78");
    enrollCourse_79(0, "Course79");
    enrollCourse_80(0, "Course80");
    enrollCourse_81(0, "Course81");
    enrollCourse_82(0, "Course82");
    enrollCourse_83(0, "Course83");
    enrollCourse_84(0, "Course84");
    enrollCourse_85(0, "Course85");
    enrollCourse_86(0, "Course86");
    enrollCourse_87(0, "Course87");
    enrollCourse_88(0, "Course88");
    enrollCourse_89(0, "Course89");
    enrollCourse_90(0, "Course90");
    enrollCourse_91(0, "Course91");
    enrollCourse_92(0, "Course92");
    enrollCourse_93(0, "Course93");
    enrollCourse_94(0, "Course94");
    enrollCourse_95(0, "Course95");
    enrollCourse_96(0, "Course96");
    enrollCourse_97(0, "Course97");
    enrollCourse_98(0, "Course98");
    enrollCourse_99(0, "Course99");
    dropCourse_0(0, "Course0");
    dropCourse_1(0, "Course1");
    dropCourse_2(0, "Course2");
    dropCourse_3(0, "Course3");
    dropCourse_4(0, "Course4");
    dropCourse_5(0, "Course5");
    dropCourse_6(0, "Course6");
    dropCourse_7(0, "Course7");
    dropCourse_8(0, "Course8");
    dropCourse_9(0, "Course9");
    dropCourse_10(0, "Course10");
    dropCourse_11(0, "Course11");
    dropCourse_12(0, "Course12");
    dropCourse_13(0, "Course13");
    dropCourse_14(0, "Course14");
    dropCourse_15(0, "Course15");
    dropCourse_16(0, "Course16");
    dropCourse_17(0, "Course17");
    dropCourse_18(0, "Course18");
    dropCourse_19(0, "Course19");
    dropCourse_20(0, "Course20");
    dropCourse_21(0, "Course21");
    dropCourse_22(0, "Course22");
    dropCourse_23(0, "Course23");
    dropCourse_24(0, "Course24");
    dropCourse_25(0, "Course25");
    dropCourse_26(0, "Course26");
    dropCourse_27(0, "Course27");
    dropCourse_28(0, "Course28");
    dropCourse_29(0, "Course29");
    dropCourse_30(0, "Course30");
    dropCourse_31(0, "Course31");
    dropCourse_32(0, "Course32");
    dropCourse_33(0, "Course33");
    dropCourse_34(0, "Course34");
    dropCourse_35(0, "Course35");
    dropCourse_36(0, "Course36");
    dropCourse_37(0, "Course37");
    dropCourse_38(0, "Course38");
    dropCourse_39(0, "Course39");
    dropCourse_40(0, "Course40");
    dropCourse_41(0, "Course41");
    dropCourse_42(0, "Course42");
    dropCourse_43(0, "Course43");
    dropCourse_44(0, "Course44");
    dropCourse_45(0, "Course45");
    dropCourse_46(0, "Course46");
    dropCourse_47(0, "Course47");
    dropCourse_48(0, "Course48");
    dropCourse_49(0, "Course49");
    dropCourse_50(0, "Course50");
    dropCourse_51(0, "Course51");
    dropCourse_52(0, "Course52");
    dropCourse_53(0, "Course53");
    dropCourse_54(0, "Course54");
    dropCourse_55(0, "Course55");
    dropCourse_56(0, "Course56");
    dropCourse_57(0, "Course57");
    dropCourse_58(0, "Course58");
    dropCourse_59(0, "Course59");
    dropCourse_60(0, "Course60");
    dropCourse_61(0, "Course61");
    dropCourse_62(0, "Course62");
    dropCourse_63(0, "Course63");
    dropCourse_64(0, "Course64");
    dropCourse_65(0, "Course65");
    dropCourse_66(0, "Course66");
    dropCourse_67(0, "Course67");
    dropCourse_68(0, "Course68");
    dropCourse_69(0, "Course69");
    dropCourse_70(0, "Course70");
    dropCourse_71(0, "Course71");
    dropCourse_72(0, "Course72");
    dropCourse_73(0, "Course73");
    dropCourse_74(0, "Course74");
    dropCourse_75(0, "Course75");
    dropCourse_76(0, "Course76");
    dropCourse_77(0, "Course77");
    dropCourse_78(0, "Course78");
    dropCourse_79(0, "Course79");
    dropCourse_80(0, "Course80");
    dropCourse_81(0, "Course81");
    dropCourse_82(0, "Course82");
    dropCourse_83(0, "Course83");
    dropCourse_84(0, "Course84");
    dropCourse_85(0, "Course85");
    dropCourse_86(0, "Course86");
    dropCourse_87(0, "Course87");
    dropCourse_88(0, "Course88");
    dropCourse_89(0, "Course89");
    dropCourse_90(0, "Course90");
    dropCourse_91(0, "Course91");
    dropCourse_92(0, "Course92");
    dropCourse_93(0, "Course93");
    dropCourse_94(0, "Course94");
    dropCourse_95(0, "Course95");
    dropCourse_96(0, "Course96");
    dropCourse_97(0, "Course97");
    dropCourse_98(0, "Course98");
    dropCourse_99(0, "Course99");
    printStudent_0();
    printStudent_1();
    printStudent_2();
    printStudent_3();
    printStudent_4();
    printStudent_5();
    printStudent_6();
    printStudent_7();
    printStudent_8();
    printStudent_9();
    printStudent_10();
    printStudent_11();
    printStudent_12();
    printStudent_13();
    printStudent_14();
    printStudent_15();
    printStudent_16();
    printStudent_17();
    printStudent_18();
    printStudent_19();
    printStudent_20();
    printStudent_21();
    printStudent_22();
    printStudent_23();
    printStudent_24();
    printStudent_25();
    printStudent_26();
    printStudent_27();
    printStudent_28();
    printStudent_29();
    printStudent_30();
    printStudent_31();
    printStudent_32();
    printStudent_33();
    printStudent_34();
    printStudent_35();
    printStudent_36();
    printStudent_37();
    printStudent_38();
    printStudent_39();
    printStudent_40();
    printStudent_41();
    printStudent_42();
    printStudent_43();
    printStudent_44();
    printStudent_45();
    printStudent_46();
    printStudent_47();
    printStudent_48();
    printStudent_49();
    printStudent_50();
    printStudent_51();
    printStudent_52();
    printStudent_53();
    printStudent_54();
    printStudent_55();
    printStudent_56();
    printStudent_57();
    printStudent_58();
    printStudent_59();
    printStudent_60();
    printStudent_61();
    printStudent_62();
    printStudent_63();
    printStudent_64();
    printStudent_65();
    printStudent_66();
    printStudent_67();
    printStudent_68();
    printStudent_69();
    printStudent_70();
    printStudent_71();
    printStudent_72();
    printStudent_73();
    printStudent_74();
    printStudent_75();
    printStudent_76();
    printStudent_77();
    printStudent_78();
    printStudent_79();
    printStudent_80();
    printStudent_81();
    printStudent_82();
    printStudent_83();
    printStudent_84();
    printStudent_85();
    printStudent_86();
    printStudent_87();
    printStudent_88();
    printStudent_89();
    printStudent_90();
    printStudent_91();
    printStudent_92();
    printStudent_93();
    printStudent_94();
    printStudent_95();
    printStudent_96();
    printStudent_97();
    printStudent_98();
    printStudent_99();
    return 0;
}